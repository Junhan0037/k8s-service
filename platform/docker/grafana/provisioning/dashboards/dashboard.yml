apiVersion: 1

# 아직 구체적인 대시보드는 없지만, 추후 JSON 파일을 추가하면 자동으로 로드된다.
providers:
  - name: ResearchEx Dashboards
    orgId: 1
    folder: ResearchEx
    type: file
    disableDeletion: false
    allowUiUpdates: true
    options:
      path: /etc/grafana/provisioning/dashboards/datasets

spring:
  application:
    name: user-portal

server:
  port: 8105

service:
  description: "User Portal 서비스는 사용자/권한/세션 관리를 위한 스켈레톤이다"

management:
  endpoints:
    web:
      exposure:
        include: "health,info"
